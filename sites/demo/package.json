{
  "name": "demo",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vitepress dev --host --port ${PORT:-5174}",
    "build": "vitepress build",
    "preview": "vitepress preview --port ${PORT:-4174}"
  },
  "dependencies": {
    "vitepress": "^1.6.4",
    "vitepress-theme-link": "workspace:*"
  }
}
