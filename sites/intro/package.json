{
  "name": "intro",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vitepress dev --host --port ${PORT:-5175}",
    "build": "vitepress build --clean-cache --clean-temp",
    "clean": "rimraf .vitepress/cache .vitepress/dist",
    "preview": "vitepress preview --port ${PORT:-4175}"
  },
  "dependencies": {
    "vitepress": "^1.6.4",
    "vitepress-theme-link": "workspace:*",
    "vitepress-plugin-callouts": "workspace:*",
    "vitepress-plugin-config": "workspace:*"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
