// import debug from 'debug'

// Create namespace for different log levels
// const base = 'vitepress-analyzer'
// const createLogger = (level: string) => debug(`${base}:${level}`)

// export const logger = {
//   info: createLogger('info'),
//   warn: createLogger('warn'),
//   error: createLogger('error'),
//   debug: createLogger('debug'),
//   perf: createLogger('perf')
// }

// // Enable all logs in development
// if (process.env.NODE_ENV === 'development') {
//   debug.enable(`${base}:*`)
// }

// Log levels can be controlled via DEBUG environment variable
// Example: DEBUG=vitepress-analyzer:info,vitepress-analyzer:error
