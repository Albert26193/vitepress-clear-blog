# pnpm requires an explicit stance on dependency build scripts. "false" means
# the scripts never run — esbuild, sharp, and @parcel/watcher all ship prebuilt
# binaries, so nothing needs to build on install.
allowBuilds:
  '@parcel/watcher': false
  esbuild: false
  sharp: false
